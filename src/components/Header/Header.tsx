import { Input } from "components";
import { StyledHeader } from "./styles";

export const Header = () => {
  return (
    <StyledHeader>
      <Input placeholder="Search" />
    </StyledHeader>
  );
};
